<h1><strong>Editing Book</strong></h1>

<!--@bookへ送るためのform_with-->
<%= form_with model: @book do |f| %>
 
 <h4>Title</h4>
 <%= f.text_field :title %>
 
 <h4>Body</h4>
 <%= f.text_area :body %>
 
 <%= f.submit 'Update Book' %>
 
<% end %>